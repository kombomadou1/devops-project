apiVersion: v1
kind: Service
metadata:
  labels:
    app: back-asso
    project: devops
  name: back-asso-service
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
    nodePort: 30000
  selector:
    app: back-asso
    project: devops
  type: NodePort
